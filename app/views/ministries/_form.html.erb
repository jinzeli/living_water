<%= form_for(@ministry) do |f| %>
  <% if @ministry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ministry.errors.count, "error") %> prohibited this ministry from being saved:</h2>

      <ul>
      <% @ministry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, "事工小组名称", class: "btn btn-info" %><br /><br />
    <%= f.text_field :name %>
  </div>
  <br />
  <div class="field">
    <%= f.label :description, "事工小组描述", class: "btn btn-info" %><br /> <br />
    <%= f.text_area :description %>
  </div>


      <%= f.label :core_coworker, "只有同工参加吗？",class: "btn btn-info" %><br /><br />
      <div class="btn-group" data-toggle="buttons-radio">
	  	  <button type="button" class="btn">
	  	  	<% if @ministry.core_coworker? %>
	  		<%= f.radio_button :core_coworker, true, checked: true %><%= label :admin, '是' %>
	  		<% else %>
	  			<%= f.radio_button :core_coworker, true %><%= label :admin, '是' %>
	  		<% end %>
	  	  </button>
	  	  <button type="button" class="btn">
	  	  	<% if @ministry.core_coworker? %>
	  	  	<%= f.radio_button :core_coworker, false %><%= label :admin, '不是' %>
	  	  	<% else %>
	  	  	<%= f.radio_button :core_coworker, false, checked: true %><%= label :admin, '不是' %>
	  	  	<% end %>
	  	  </button>
	  </div>  
  <div class="actions" >
  	<br />
    <%= f.submit "更新", class: "btn btn-success" %>
  </div>
<% end %>
