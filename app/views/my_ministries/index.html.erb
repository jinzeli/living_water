
<div class="row">
		<div class="span10 offset1">
<div class="well">
	<h1>爱统计</h1>
	<br /><br />
	<h5>共有 <span class="label label-warning"><%= @ministries.count %></span> 个事工小组。
	有 <span class="label label-warning"><%= @my_ministries.count %></span> 人次参与服事。参与服事的比例为
		<span class="label label-warning">
			<%= ((@joined_ministry_person_count.to_f)*100/@coworkers.count).round(2) unless @coworkers.count==0 %>%
		</span>。</h5>
	
	
	<br /><br />
<table class="table">
  <tr>
    <th>团契事工</th>
    <th>爱服侍成员</th>

  </tr>

<% @ministries.each do |ministry| %>
	<% ministry.coworkers.each do |coworker| %>
	<% unless (ministry.core_coworker? && current_coworker.core_coworker? == false) %>
	<tr>
		<td><%= ministry.name %></td>
		<td><%= coworker.name %></td>
	</tr>
	<% end %>
	<% end %>
<% end %>
  <tr>
  	<td></td>
  	<td></td>

  </tr>
</table>
</div>
</div></div>
<!--<%= link_to 'New My ministry', new_my_ministry_path %>-->
